type: object
required:
  - hits
properties:
  hits:
    type: object
    properties:
      total:
        type: object
        properties:
          value:
            type: integer
          relation:
            type: string
        required:
          - value
          - relation
      hits:
        type: array
        items:
          type: object
    required: 
      - hits
      - total
      - max_score